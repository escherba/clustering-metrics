[aliases]
test=pytest

[tool:pytest]
addopts = --doctest-modules --cache-clear
testpaths = 
    clustering_metrics
    pymaptools
    tests

coverage:run]
branch = True
include =
    clustering_metrics
    pymaptools
omit =
    *tests*
    *_test.py
